import type { SlotDataItem } from '@comp/data-table'

const list: SlotDataItem[] = [
  { name: 'leading', description: '前置组件插槽' },
  { name: 'trailing', description: '后置组件插槽' },
]

export default list
