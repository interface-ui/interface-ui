import type { SlotDataItem } from '@comp/data-table'

const list: SlotDataItem[] = [
  { name: 'leading', description: 'Leading component' },
  { name: 'trailing', description: 'Trailing component' },
]

export default list
