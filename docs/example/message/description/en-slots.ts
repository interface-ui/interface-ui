import type { SlotTableItem } from '../../../.vitepress/components/table-block/src/table'

const list: SlotTableItem[] = [{ name: 'default', remark: 'customize default content' }]

export default list
