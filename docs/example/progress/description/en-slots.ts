import type { SlotTableItem } from '../../../.vitepress/components/table-block/src/table'

const list: SlotTableItem[] = [{ name: 'default', remark: 'Customize the default content' }]

export default list
