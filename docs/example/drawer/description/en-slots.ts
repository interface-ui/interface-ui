import type { SlotDataItem } from '@comp/data-table'

const list: SlotDataItem[] = [
  { name: 'fixedArea', description: 'Customize the front content' },
  { name: 'closeArea', description: 'Customize the closed content' },
]

export default list
